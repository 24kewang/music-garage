import type { GameManifest } from "@/games/types";

export const manifest: GameManifest = {
  slug: "music",
  title: "MUSIC",
  blurb: "HORSE for musicians. Copy the melody or take a letter.",
  iconId: "basketball",
  status: "playable",
  minPlayers: 2,
  maxPlayers: 4,
};

export default manifest;
